import 'package:flutter/material.dart';

class Speach extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Speech Control"),
      ),
      body: Center(),
    );
  }
}
